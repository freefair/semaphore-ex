# Full upstream sync

Merge the assessed docs and root SHAs without rewriting either history. Both local merges are prepared before publication because new CLI/config generators must describe the selected merged product. Publish docs before the root submodule pointer after approval.

1. Preserve unrelated untracked artifacts with checksums and retain recovery branches (complete).
2. Retain preflight identities, schema/seam diffs, conflict previews (complete; no incoming migrations or contract changes).
3. Resolve docs reorganization preserving canonical security fallbacks and full-product identity; integrate upstream navigation and generators.
4. Resolve root conflicts preserving debug reload, source maps, stream callback binding, and security calls.
5. Run focused checks, exact-upstream failure reproduction if needed, serial eleven-locale docs build, full retained product gates and browser QA; dedicated Terra review of final diff.
6. Commit verified docs, then root pointer. Request concrete publication approval, fetch and ordinary push docs then root, verify exact-head CI.
7. Restore preserved unrelated files with original checksums. Retain evidence/recovery branches.
